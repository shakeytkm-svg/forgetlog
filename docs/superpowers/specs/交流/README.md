# 交流 / Trae 提示词

**分工**：我（pi）写提示词 -> 你喂给 Trae -> Trae 写代码 -> 把代码贴回给我 -> 我审核 -> 通过再进下一条。

## 怎么用

1. 按 `01` -> `06` 顺序，**一次只喂一条**给 Trae（让 Trae 在 ForgetLog 项目目录里工作）。
2. Trae 写完后，把生成的文件内容（或关键代码）贴回给我。
3. 我对照该条的「验收标准」审核：对不对、坑在哪、要不要改。
4. 通过后再进下一条。

## 总约束（每条提示词都默认遵守，不用重复跟 Trae 说）

- 纯 **vanilla JS**，不引框架（React / Vue / jQuery 都不要）。
- **无构建步骤**（不装 npm / Vite / webpack）。
- **必须用本地服务器跑**：`python -m http.server 8000` 或 VS Code Live Server，访问 `http://localhost:8000`。**别用 `file://` 直接打开**——Service Worker 在 file:// 下无法注册，IndexedDB 也受限，离线和「安装」都会失败。所有验收都在 localhost 下做。
- 存储用 **IndexedDB**（不用 localStorage）。
- 单文件夹项目，文件平铺，别套深目录。
- **中文界面**，**移动端优先**（竖屏、大按钮、触摸友好、字号别太小）。
- 代码加中文注释。
- **命名约定**：时间字段统一用 `created_at`（下划线），全项目一致；别用驼峰 `createdAt`，别混用。
- 一条提示词只做它说的事，别自作主张加别的功能。

## 提示词清单

| 顺序 | 文件 | 做什么 |
|---|---|---|
| 01 | `01-PWA骨架与存储.md` | 建项目骨架 + IndexedDB 库 |
| 02 | `02-数据层CRUD.md` | entries 的增删查统计 |
| 03 | `03-主页与记录页.md` | 屏1主页 + 屏2记录页（文字） |
| 04 | `04-语音输入.md` | 记录页加按住说话转文字 |
| 05 | `05-切场检查与看板.md` | 切场检查弹窗 + 屏3看板 |
| 06 | `06-唤醒与收尾.md` | 语音口令唤醒 + 离线/可装打磨 |

## 审核约定

你把 Trae 的产出贴回来时，最好带上：① 改了/新建了哪些文件；② 关键代码段。我会照「验收标准」逐条对，告诉你过不过、哪里要改。改完 Trae 再交，直到过，再进下一条。
